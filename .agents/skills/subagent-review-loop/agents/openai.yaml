# FILE: .agents/skills/subagent-review-loop/agents/openai.yaml
# VERSION: 1.0.0
# START_MODULE_CONTRACT
#   PURPOSE: Expose UI metadata for the project-local subagent review loop skill.
#   SCOPE: Covers display name, short description, and default invocation prompt; excludes runtime review workflow logic.
#   DEPENDS: .agents/skills/subagent-review-loop/SKILL.md.
#   LINKS: M-SUBAGENT-REVIEW-LOOP-SKILL / V-M-SUBAGENT-REVIEW-LOOP-SKILL.
#   ROLE: CONFIG
#   MAP_MODE: SUMMARY
# END_MODULE_CONTRACT
# START_MODULE_MAP
#   interface - Defines Codex UI metadata and default prompt for the skill.
# END_MODULE_MAP
# START_CHANGE_SUMMARY
#   LAST_CHANGE: 1.0.0 - Added metadata for the project-local subagent review loop skill.
# END_CHANGE_SUMMARY

interface:
  display_name: 'Subagent Review Loop'
  short_description: 'Iterative artifact review by subagents.'
  default_prompt: 'Use $subagent-review-loop to review and refine this spec until independent reviewers approve it.'
