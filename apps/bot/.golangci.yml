version: '2'

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosec
    - misspell
    - unconvert

formatters:
  enable:
    - goimports
  settings:
    goimports:
      local-prefixes:
        - monorepo-template
