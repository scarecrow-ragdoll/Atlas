# FILE: apps/web-admin/src/entities/admin-auth/api/currentAdmin.graphql
# VERSION: 1.0.0
# START_MODULE_CONTRACT
#   PURPOSE: Define the web-admin GraphQL current-admin operation document for cookie-backed admin authentication.
#   SCOPE: Owns the CurrentAdmin query document for codegen and runtime imports; excludes transport, schema ownership, and UI rendering.
#   DEPENDS: libs/graphql/schema/admin_auth.graphql, tools/codegen/codegen.ts.
#   LINKS: M-WEB-ADMIN / M-GRAPHQL-SCHEMA / V-M-WEB-ADMIN.
#   ROLE: CONFIG
#   MAP_MODE: SUMMARY
# END_MODULE_CONTRACT
# START_MODULE_MAP
#   CurrentAdmin - Reads the current admin principal from the session cookie.
# END_MODULE_MAP
# START_CHANGE_SUMMARY
#   LAST_CHANGE: 1.0.0 - Split CurrentAdmin into a single-operation document for login route guard runtime use.
# END_CHANGE_SUMMARY

query CurrentAdmin {
  me {
    id
    email
    name
    role
    createdAt
    updatedAt
  }
}
