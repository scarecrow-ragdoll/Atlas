# FILE: apps/web-admin/src/entities/user/api/user.graphql
# VERSION: 1.0.0
# START_MODULE_CONTRACT
#   PURPOSE: Define the admin GetUser GraphQL operation for user detail pages.
#   SCOPE: Owns the codegen-visible detail query document imported by the Vite admin user-detail route; excludes runtime transport, schema ownership, and UI rendering.
#   DEPENDS: apps/api/internal/graph/schema.graphqls, tools/codegen/codegen.ts, apps/web-admin/src/pages/user-detail-page.tsx.
#   LINKS: M-WEB-ADMIN / M-GRAPHQL-SCHEMA / V-M-WEB-ADMIN
#   ROLE: RUNTIME
#   MAP_MODE: SUMMARY
# END_MODULE_CONTRACT
# START_MODULE_MAP
#   GetUser - Fetches one user's identity and timestamps for the admin user-detail route.
# END_MODULE_MAP
# START_CHANGE_SUMMARY
#   LAST_CHANGE: 1.0.0 - Added file-local GRACE markup for Task 8 governance scan.
# END_CHANGE_SUMMARY

query GetUser($id: UUID!) {
  user(id: $id) {
    id
    email
    name
    createdAt
    updatedAt
  }
}
