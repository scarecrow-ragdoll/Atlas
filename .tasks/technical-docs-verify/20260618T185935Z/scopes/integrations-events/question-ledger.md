# Integrations-Events Question Ledger

| ID | Scope | Severity | Parent | Question | Why It Matters | Needed Artifact Or Decision | Source Or Report | Status | Resolution |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TQ-INT-001 | integrations-events | dev-blocking | none | Is AI Export ZIP generation synchronous or asynchronous, and what progress reporting mechanism is used for large exports? | Performance targets specify "best effort, show progress" for 12-month exports with photos, implying async with progress, but no mechanism is defined. | Decision: sync vs async; progress API or event contract. | product-brief.md §AI Export; worker-attempt-1 | open | TBD |
| TQ-INT-002 | integrations-events | dev-blocking | none | Is Backup ZIP generation synchronous or asynchronous? | Backup with media is "best effort (media-size dependent)" with no progress or timeout contract. | Decision: sync vs async; timeout and progress contract. | product-brief.md §Backup; worker-attempt-1 | open | TBD |
| TQ-INT-003 | integrations-events | dev-blocking | none | Is Backup Import synchronous or asynchronous, and what happens on mid-import failure beyond "no silent partial import"? | EDGE-021 calls out mid-import failure with "transaction rollback undefined" and the requirement says "complete success or clean failure" but no recovery/rollback mechanism is specified. | Import transaction and rollback specification; async vs sync decision. | edge-cases.md EDGE-021; worker-attempt-1 | open | TBD |
| TQ-INT-004 | integrations-events | needs-owner-decision | none | What are the file size limits and disk space pre-checks for AI Export and Backup ZIP generation? | EDGE-024 (disk full during export) has no quota handling or pre-check defined. Without limits, operations can fail mid-way without user feedback. | File size limits or pre-check policy; disk space validation contract. | edge-cases.md EDGE-024; worker-attempt-1 | open | TBD |
| TQ-INT-005 | integrations-events | needs-owner-decision | none | What is the session timeout for PIN guard, and how does it interact with long-running export/import operations? | Export and import operations can exceed typical session TTL; if session expires mid-operation, the operation may fail inconsistently. | Session TTL specification; session-extension policy for long operations. | edge-cases.md EDGE-012; functional-spec.md §7.2; worker-attempt-1 | open | TBD |
| TQ-INT-006 | integrations-events | deferred | none | What rate limits or abuse prevention apply to export/backup operations (min interval between exports, max concurrent, etc.)? | Manual copy-paste AI workflow implies low request rate, but no guardrail is specified. Low risk for single-user MVP. | Optional rate-limit or cooldown policy. | worker-attempt-1 | open | TBD |
| TQ-INT-007 | integrations-events | watchlist | TQ-INT-001 | If progress reporting uses Redis (polling endpoint), what is the behavior when Redis is unavailable during export/import? | Redis progress coupling means Redisis unavailability (EDGE-023) blocks progress visibility. Operation must not silently hang or crash. | Fallback behavior specification for Redis-dependent progress tracking. | worker-attempt-2 | open | TBD |
| TQ-INT-008 | integrations-events | watchlist | TQ-INT-001 | What is the ZIP generation memory strategy (in-memory, streaming, or temp-file) for large exports with photos? | 12-month exports with photos can exceed available RAM if fully buffered. | Memory budget or streaming approach for ZIP generation. | worker-attempt-2 | open | TBD |